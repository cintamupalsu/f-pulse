<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <link rel="icon" type="image/png" href="/assets/logos/kalpataru.png">
    <%= render 'layouts/shim' %>
   
    
    <!--
    <style>
        .flash-warning   { color: red; }
        .flash-notice  { color: blue; }
        .flash-success { color: green; }
    </style>
    -->
  </head>

  <body>
    <%= render 'layouts/header' %>
    <div class='container'>
      <% if user_signed_in? %>
        <%= render 'layouts/messages' %>
      <% end %>
      <%= yield %>
    </div>
    <%= render 'layouts/footer' %>
    <div class='container'>
      <%= debug(params) if Rails.env.develompent? %>
    </div>
  </body>
</html>
